spring:
  neo4j:
    uri: bolt://neo4j:7687
    authentication:
      username: neo4j
      password: password

  datasource:
    url: jdbc:sqlserver://sqlserver:1433;databaseName=twitterlike;encrypt=true;trustServerCertificate=true
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
    username: sa
    password: StrongP@ssw0rd!

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  sql:
    init:
      mode: always
